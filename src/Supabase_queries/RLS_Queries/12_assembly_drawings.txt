-- Enable RLS for assembly_drawings
ALTER TABLE public.assembly_drawings ENABLE ROW LEVEL SECURITY;

-- Create policy for all operations for all authenticated users
CREATE POLICY "All authenticated users can manage assembly drawings"
    ON public.assembly_drawings
    FOR ALL
    USING (auth.role() = 'authenticated')
    WITH CHECK (auth.role() = 'authenticated');